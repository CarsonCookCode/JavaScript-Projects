var A = "These are \"words\" aren\'t they?", C = "...They are indeed."; //Declaring multiple variables and assigning string values.
var D = A + C; //Declaring another variable and concatenating two strings.
D = D.fontcolor("blue"); //Setting the property of fontcolor of D to blue.
document.write(D); //Write D on the page.
var B = 7 + 5; //Declaring a variable and performing an expression.
window.alert(B); //Call alert method from window object with the value of B.
